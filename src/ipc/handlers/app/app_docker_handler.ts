/**
 * Docker-specific app execution helpers are registered through app execution handlers.
 * There are currently no standalone app Docker IPC endpoints to register.
 */

export function registerAppDockerHandlers() {}
